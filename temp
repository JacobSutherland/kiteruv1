 const options = document.createElement('div');
    options.classList.add('answer-container');
        const option1 = document.createElement('p');
        const option2 = document.createElement('p');
        const option3 = document.createElement('p');
        const option4 = document.createElement('p');

        option1.classList.add('answer');
        option2.classList.add('answer');
        option3.classList.add('answer');
        option4.classList.add('answer');

        option1.textContent = `${characters[questionRandomizer()].sound}`
        option2.textContent = `${characters[questionRandomizer()].sound}`
        option3.textContent = `${characters[questionRandomizer()].sound}`
        option4.textContent = `${characters[questionRandomizer()].sound}`

        options.appendChild(option1);
        options.appendChild(option2);
        options.appendChild(option3);
        options.appendChild(option4);

        or

        const options = document.createElement('div');
    options.classList.add('answer-container');
    for(let i = 0; i < 4; i++){
        const option = document.createElement('p');
        option[i].classList.add('answer');
        option[i].textContent = `${characters[questionRandomizer()].sound}`
        options.appendChild(option[i]);
    }

    cardSection.removeChild(cardSection.firstElementChild)
        button.textContent = "Next Kana"
        game.insertAdjacentHTML('beforeend', card)
        game.insertAdjacentHTML('beforeend', opttions)